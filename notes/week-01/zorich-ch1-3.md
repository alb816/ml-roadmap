* Отображение из множества X в множество Y - это правило задающее каждому х единственный у

* Композиция функции f и g это g o f = g(f(x))

* Мощность множества это кол-во элементов в нем

* Предел функции это число А такое что: 
для всех е>0 существует б>0 что для всех x в 
E(0 < |x-a| < б => |f(x) - A| < e)

* Предел последовательности x_n это число А такое что: 
для всех е>0 существует n0 в N что для всех n > n0 (|x_n - A| < e)

* Непрерывность f:E->R в точке a(пред) в E  :=
для всех е>0 существует б>0 что для всех x в 
E(0 < |x-a| < б => |f(x) - f(a)| < e)
* Точки разрыва:
    1. 1-го рода: когда левый и правый предел кончены и хотя бы один из них не равен f(a)
    2. 2-го рода: когда не существует хотя бы одного из пределов в определении выше.
    3. Устранимая точка разрыва: $\lim_{x\to\ a} f(x) \ne f(a)$ и при этом $\lim_{x\to a+0} f(x) = L = \lim_{x\to a-0} f(x)$
    4. Бесконечного разрыва: если хотя бы один из них равен ${\infty }$